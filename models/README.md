# Папка моделей

Скрипт `ml/train_baseline.py` сохраняет сюда обученный пайплайн (`baseline.joblib`) и файл метаданных (`metadata.json`).
Если файлов нет, backend автоматически использует встроенную keyword-модель, но для более точных предсказаний рекомендуется обучить настоящую ML-модель.
